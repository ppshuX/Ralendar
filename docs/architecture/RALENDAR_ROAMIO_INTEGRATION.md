# 🏗️ Ralendar + Roamio 跨应用集成架构

> **真实架构**：独立服务 + API 通信

---

## 📊 **实际架构图**

```
┌─────────────────────────────────────────────────────────┐
│              腾讯云服务器（app7626）                     │
│              Ralendar 后端服务                          │
├─────────────────────────────────────────────────────────┤
│  📦 应用：Django + uWSGI + Nginx                        │
│  🗄️ 数据库：SQLite (db.sqlite3)                        │
│      ├── auth_user              - Ralendar 用户        │
│      ├── allauth_socialaccount  - OAuth 账号           │
│      ├── api_event              - 日程事件             │
│      ├── calendar_holidays      - 节假日               │
│      ├── calendar_lunar         - 黄历                 │
│      └── calendar_fortunes      - 运势                 │
│                                                         │
│  🌐 API：/api/v1/fusion/*       - Fusion API          │
└─────────────────────────────────────────────────────────┘
                         ↑
                         │ HTTPS / REST API
                         │ JWT Token + UnionID
                         │
┌─────────────────────────────────────────────────────────┐
│              阿里云服务器（Roamio）                      │
│              Roamio 后端服务                            │
├─────────────────────────────────────────────────────────┤
│  📦 应用：Django + uWSGI + Nginx                        │
│  🗄️ 数据库：MySQL (roamio_production)                  │
│      ├── auth_user              - Roamio 用户          │
│      ├── socialaccount          - OAuth 账号           │
│      ├── roamio_trip            - 旅行计划             │
│      ├── roamio_post            - 内容发布             │
│      └── ...                                           │
│                                                         │
│  🌐 调用：Ralendar Fusion API   - 创建日程             │
└─────────────────────────────────────────────────────────┘
```

---

## 🎯 **核心设计原则**

### **1. 完全解耦**
- ✅ 两个应用在**不同服务器**上独立运行
- ✅ 各自有**独立的数据库**
- ✅ 通过 **RESTful API** 通信
- ✅ 不直接访问对方的数据库

### **2. 统一身份**
- ✅ 使用 **QQ OAuth** 登录
- ✅ 通过 **UnionID** 识别同一用户
- ✅ 不需要同步用户数据

### **3. 数据隔离**
- ✅ Ralendar 的数据存在 Ralendar 服务器
- ✅ Roamio 的数据存在 Roamio 服务器
- ✅ 需要对方数据时通过 API 调用

---

## 🔄 **跨应用工作流程**

### **场景：用户在 Roamio 创建旅行计划，同步到 Ralendar**

```
┌────────────────────────────────────────────────────────┐
│ 1️⃣ 用户在 Roamio 前端创建旅行                          │
│    "2025-12-01 去上海旅行"                             │
└──────────────────┬─────────────────────────────────────┘
                   │
                   ▼
┌────────────────────────────────────────────────────────┐
│ 2️⃣ Roamio 后端调用 Ralendar Fusion API                │
│    POST /api/v1/fusion/events/                         │
│    Header: Authorization: Bearer {jwt_token}           │
│    Body: {                                             │
│      "unionid": "UID_55F2CB8DE6029B9400755BC4224969B5",│
│      "title": "去上海旅行",                            │
│      "start_time": "2025-12-01T09:00:00Z",             │
│      "source_app": "roamio",                           │
│      "related_trip_slug": "shanghai-trip-2025"         │
│    }                                                   │
└──────────────────┬─────────────────────────────────────┘
                   │
                   ▼
┌────────────────────────────────────────────────────────┐
│ 3️⃣ Ralendar 后端处理请求                               │
│    a) 验证 JWT Token                                   │
│    b) 从 UnionID 找到/创建本地用户                     │
│       - SocialAccount.objects.filter(                  │
│           unionid="UID_55F..."                         │
│         ).first()                                      │
│       - 如果找到：user_id=2                            │
│       - 如果没找到：创建新用户                         │
│    c) 创建事件                                         │
│       - Event.objects.create(                          │
│           user_id=2,                                   │
│           title="去上海旅行",                          │
│           source_app="roamio",                         │
│           ...                                          │
│         )                                              │
└──────────────────┬─────────────────────────────────────┘
                   │
                   ▼
┌────────────────────────────────────────────────────────┐
│ 4️⃣ 返回响应给 Roamio                                   │
│    {                                                   │
│      "id": 123,                                        │
│      "title": "去上海旅行",                            │
│      "created_at": "2025-11-10T12:00:00Z"              │
│    }                                                   │
└──────────────────┬─────────────────────────────────────┘
                   │
                   ▼
┌────────────────────────────────────────────────────────┐
│ 5️⃣ 用户在 Ralendar 看到这个事件 ✅                     │
│    - 来源标记："来自 Roamio"                           │
│    - 可以跳转回 Roamio 查看详情                        │
└────────────────────────────────────────────────────────┘
```

---

## 🔑 **UnionID 是如何工作的？**

### **QQ OAuth 返回的身份信息**

```json
// Roamio 用户登录 QQ
{
  "openid": "OPENID_ABC123",           // Roamio 内唯一
  "unionid": "UID_55F2CB8DE6029B..."   // 跨应用唯一 ⭐
}

// Ralendar 用户登录 QQ（同一个 QQ 号）
{
  "openid": "OPENID_XYZ789",           // Ralendar 内唯一
  "unionid": "UID_55F2CB8DE6029B..."   // 同一个 UnionID！⭐
}
```

### **UnionID 的作用**

- ✅ **同一个 QQ 号**在 Roamio 和 Ralendar 会有**不同的 OpenID**
- ✅ 但会有**相同的 UnionID**
- ✅ Ralendar 通过 UnionID 识别："哦，这是同一个用户！"

---

## 📊 **数据存储说明**

### **Ralendar 数据库（SQLite）**

| 表名 | 用途 | 数据来源 |
|-----|------|---------|
| `auth_user` | 用户账号 | Ralendar 本地 |
| `allauth_socialaccount` | OAuth 账号（含 UnionID） | QQ OAuth |
| `api_event` | 日程事件 | Ralendar 创建 + Roamio Fusion API |
| `calendar_holidays` | 节假日 | Ralendar 本地导入 |
| `calendar_lunar` | 黄历 | Ralendar 本地导入 |
| `calendar_fortunes` | 运势 | Ralendar 本地/API 获取 |

### **Roamio 数据库（MySQL）**

| 表名 | 用途 | 数据来源 |
|-----|------|---------|
| `auth_user` | 用户账号 | Roamio 本地 |
| `socialaccount` | OAuth 账号（含 UnionID） | QQ OAuth |
| `roamio_trip` | 旅行计划 | Roamio 创建 |
| `roamio_post` | 内容发布 | Roamio 创建 |

**关键：** 两个数据库中的 `auth_user` 表中，同一个人的 `user_id` **不同**！

```
Ralendar: User(id=2, username="张三", unionid="UID_123")
Roamio:   User(id=5, username="张三", unionid="UID_123")

不同的 ID，但同样的 UnionID！
```

---

## 🚀 **Fusion API 端点**

### **事件管理**

| 方法 | 端点 | 说明 |
|-----|------|------|
| `POST` | `/api/v1/fusion/events/` | 创建事件 |
| `POST` | `/api/v1/fusion/events/batch/` | 批量创建 |
| `GET` | `/api/v1/fusion/events/` | 获取用户所有事件 |
| `GET` | `/api/v1/fusion/events/{id}/` | 获取单个事件 |
| `PUT` | `/api/v1/fusion/events/{id}/` | 更新事件 |
| `DELETE` | `/api/v1/fusion/events/{id}/` | 删除事件 |

### **请求示例**

```bash
# Roamio 调用 Ralendar
curl -X POST "https://app7626.acapp.acwing.com.cn/api/v1/fusion/events/" \
  -H "Authorization: Bearer ${JWT_TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{
    "unionid": "UID_55F2CB8DE6029B9400755BC4224969B5",
    "title": "去上海旅行",
    "start_time": "2025-12-01T09:00:00Z",
    "end_time": "2025-12-03T18:00:00Z",
    "description": "上海三日游",
    "location": "上海市",
    "latitude": 31.2304,
    "longitude": 121.4737,
    "source_app": "roamio",
    "related_trip_slug": "shanghai-2025"
  }'
```

---

## 🔒 **安全性**

### **认证**

1. **JWT Token**：每个请求必须携带有效的 JWT Token
2. **UnionID 验证**：确保 UnionID 与 Token 中的用户匹配
3. **权限检查**：用户只能操作自己的数据

### **示例代码**

```python
# backend/api/views/fusion.py

from rest_framework.permissions import IsAuthenticated
from rest_framework.decorators import api_view, permission_classes

@api_view(['POST'])
@permission_classes([IsAuthenticated])  # 必须登录
def batch_create_events(request):
    # 1. 从请求中提取 UnionID
    unionid = request.data.get('unionid')
    
    # 2. 查找本地用户
    social = SocialAccount.objects.filter(unionid=unionid).first()
    if not social:
        # 如果没找到，创建新用户
        user = User.objects.create_user(username=f"user_{unionid[:8]}")
        social = SocialAccount.objects.create(
            user=user,
            provider='qq',
            unionid=unionid,
            openid=request.data.get('openid', '')
        )
    
    user = social.user
    
    # 3. 创建事件（确保 user 正确）
    for event_data in request.data.get('events', []):
        Event.objects.create(
            user=user,  # ← 使用本地用户 ID
            title=event_data['title'],
            start_time=event_data['start_time'],
            source_app='roamio',
            ...
        )
```

---

## ✅ **优势**

| 方面 | 优势 |
|-----|-----|
| **独立性** | 一个服务挂了不影响另一个 |
| **扩展性** | 可以独立扩展、迁移 |
| **技术自由** | 各自选择合适的数据库和技术栈 |
| **数据安全** | 数据隔离，不会互相干扰 |
| **维护性** | 独立部署、独立更新 |

---

## 📝 **常见误解**

### ❌ **误解 1：Ralendar 和 Roamio 共享数据库**

**事实：** 它们在**不同服务器**，有**独立数据库**，通过 **API** 通信。

### ❌ **误解 2：需要同步用户数据**

**事实：** 不需要！通过 **UnionID** 即可识别同一用户，各自维护自己的用户表。

### ❌ **误解 3：需要复杂的多数据库配置**

**事实：** 每个应用只需要配置**一个数据库**，不需要多数据库路由器。

---

## 🔮 **未来扩展**

### **添加更多应用**

```
Ralendar (日历)  ←┐
                  ├─→ Fusion API ←→ 各应用通过 API 通信
Roamio (旅行)   ←┤
                  │
Rote (笔记)     ←┤
                  │
Routes (地图)   ←┘
```

所有应用：
- ✅ 独立服务器（可选）
- ✅ 独立数据库
- ✅ 通过 UnionID 识别用户
- ✅ 通过 Fusion API 互通数据

---

## 📞 **联系方式**

**架构维护者**: Ralendar + Roamio 核心团队  
**问题反馈**: 在项目根目录创建 Issue

---

**🎉 这才是真正的微服务架构：松耦合、强协作！**

