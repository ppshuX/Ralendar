=================================================================
🚀 Ralendar AI功能部署命令（复制到服务器执行）
=================================================================

# 1. SSH连接到服务器
ssh root@81.71.138.122

# 2. 进入项目目录
cd ~/kotlin_calendar

# 3. 拉取最新代码
git pull origin master

# 4. 配置API Key（重要！）
cd backend
echo "QWEN_API_KEY=sk-0b9ac4fb62f640e2aeb473f1cc30d34e" >> .env

# 5. 激活虚拟环境
source ~/kotlin_calendar/venv/bin/activate

# 6. 收集静态文件
python manage.py collectstatic --noinput

# 7. 重启服务
sudo systemctl restart gunicorn

# 8. 检查服务状态
sudo systemctl status gunicorn --no-pager | head -n 10

# 9. 查看日志（如果有问题）
tail -f /var/log/gunicorn/error.log

=================================================================
✅ 部署完成后测试AI API
=================================================================

# 测试1：AI对话
curl -X POST https://app7626.acapp.acwing.com.cn/api/ai/chat/ \
  -H "Content-Type: application/json" \
  -d '{"message":"你好","context":{"current_date":"2025-11-12"}}'

# 测试2：解析日程
curl -X POST https://app7626.acapp.acwing.com.cn/api/ai/parse-event/ \
  -H "Content-Type: application/json" \
  -d '{"text":"明天下午3点开会"}'

# 测试3：检查节日API
curl "https://app7626.acapp.acwing.com.cn/api/holidays/check/?date=2025-10-01"

=================================================================
⚠️ 安全提示
=================================================================

- API Key已经配置到 .env 文件
- .env 文件不会被Git追踪（已在.gitignore中）
- 请勿在公开的地方分享API Key
- 建议定期检查API使用量：https://dashscope.aliyun.com/

=================================================================

